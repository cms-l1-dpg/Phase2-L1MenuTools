JetMatchingBarrelSC8:
  sample: TT
  version: V49nano_RelVal
  match_test_to_ref: True
  reference_object:
    object: "GenJetAK8"
    x_arg: "pt"
    label: "Gen AK8 Jets"
    cuts:
      event:
        - "abs({eta}) < 1.5"
      object:
        - "abs({eta}) < 2.4"
  test_objects:
    L1puppiJetSC8:default:barrel: "pt"
    L1puppiJetSC8:mass30:barrel: "pt"
  xlabel: "Gen. $p_T$ (GeV)"
  ylabel: "Matching Efficiency (<threshold> GeV, barrel)"
  binning:
    min: 0
    max: 500
    step: 10

JetMatchingEndcapSC8:
  sample: TT
  version: V49nano_RelVal
  match_test_to_ref: True
  reference_object:
    object: "GenJetAK8"
    x_arg: "pt"
    label: "Gen AK8 Jets"
    cuts:
      event:
        - "abs({eta}) > 1.5"
      object:
        - "abs({eta}) < 2.4"
  test_objects:
    L1puppiJetSC8:default:endcap: "pt"
    L1puppiJetSC8:mass30:endcap: "pt"
  xlabel: "Gen. $p_T$ (GeV)"
  ylabel: "Matching Efficiency (<threshold> GeV, endcap)"
  binning:
    min: 0
    max: 500
    step: 10

JetMatchingForwardHGCSC8:
  sample: TT
  version: V49nano_RelVal
  match_test_to_ref: True
  reference_object:
    object: "GenJetAK8"
    x_arg: "pt"
    label: "Gen AK8 Jets"
    cuts:
      event:
        - "abs({eta}) > 2.4"
      object:
        - "abs({eta}) < 3.0"
  test_objects:
    L1puppiJetSC8:default:forwardHGC: "pt"
    L1puppiJetSC8:mass30:forwardHGC: "pt"
  xlabel: "Gen. $p_T$ (GeV)"
  ylabel: "Matching Efficiency (<threshold> GeV, forwardHGC)"
  binning:
    min: 0
    max: 500
    step: 10

JetMatchingForwardHFSC8:
  sample: TT
  version: V49nano_RelVal
  match_test_to_ref: True
  reference_object:
    object: "GenJetAK8"
    x_arg: "pt"
    label: "Gen AK8 Jets"
    cuts:
      event:
        - "abs({eta}) > 3.0"
      object:
        - "abs({eta}) < 5.0"
  test_objects:
    L1puppiJetSC8:default:forwardHF: "pt"
    L1puppiJetSC8:mass30:forwardHF: "pt"
  xlabel: "Gen. $p_T$ (GeV)"
  ylabel: "Matching Efficiency (<threshold> GeV, forwardHF)"
  binning:
    min: 0
    max: 500
    step: 10
